import { BlogPost } from "../../../../components/blog/blog_post";
import { Subheading } from "../../../../components/blog/subheading";
import { SideBySide } from "../../../../components/blog/sidebyside";
import { Status } from "../../../../components/blog/status";
import { Note } from "../../../../components/blog/note";
import { TOC } from "../toc";

import { meta } from "./meta";
export { meta } from "./meta";
const CURRENT_PART = 8;

export default ({ children }) => <BlogPost meta={meta}>{children}</BlogPost>;

<TOC current={CURRENT_PART} />

---

Before using TypeScript, I never thought about all the limitations of CSS.

And honestly, if I haven't started using TypeScript, I would probably still think that _CSS Modules & SCSS is all we need, because we can't do any better_. But we do.

```

```
